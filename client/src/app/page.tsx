import { NextPage } from 'next';

const Home: NextPage = () => {
    return (
        <div>
            <h1>Home</h1>
            <button className="btn btn-primary">Button</button>
        </div>
    );
};

export default Home;
